# ===============================================
# 부성스틸 HR 시스템 - 환경변수 템플릿
# ===============================================
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값으로 변경하세요.

# ==================== MongoDB ====================
# MongoDB 연결 URI
# 로컬 개발: mongodb://127.0.0.1:27017/busung_hr
# MongoDB Atlas: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/busung_hr
MONGODB_URI=mongodb://127.0.0.1:27017/busung_hr

# 대체 환경변수 (MONGO_URI와 MONGODB_URI 둘 다 지원)
# MONGO_URI=mongodb://127.0.0.1:27017/busung_hr

# 데이터베이스 이름
DB_NAME=busung_hr

# ==================== 서버 설정 ====================
# Node 환경 (development / production)
NODE_ENV=development

# 서버 포트
PORT=5000

# 시간대 설정 (한국 시간)
TZ=Asia/Seoul

# ==================== CORS 설정 ====================
# 허용할 클라이언트 오리진 (쉼표로 구분)
# 예: http://localhost:3000,http://localhost:3001
ALLOWED_ORIGINS=http://localhost:3000

# ==================== JWT 인증 (선택사항) ====================
# JWT Secret Key (보안을 위해 복잡한 문자열 사용)
# JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT 만료 시간 (예: 1h, 7d, 30d)
# JWT_EXPIRES_IN=7d

# ==================== 파일 업로드 설정 ====================
# 업로드 파일 최대 크기 (MB)
MAX_FILE_SIZE=10

# 업로드 디렉토리
UPLOAD_DIR=uploads

# ==================== 로깅 설정 ====================
# 로그 레벨 (error, warn, info, debug)
LOG_LEVEL=info

# ==================== 백업 설정 ====================
# 자동 백업 활성화 (true / false)
AUTO_BACKUP_ENABLED=true

# 백업 주기 (cron 표현식: "0 2 * * *" = 매일 새벽 2시)
BACKUP_SCHEDULE=0 2 * * *

# 백업 디렉토리
BACKUP_DIR=backups

# 백업 보관 기간 (일)
BACKUP_RETENTION_DAYS=30

# ==================== Socket.io 설정 ====================
# Socket.io CORS 설정
SOCKET_CORS_ORIGIN=*

# ==================== 외부 API 설정 (선택사항) ====================
# 공휴일 API 키 (한국천문연구원 등)
# HOLIDAY_API_KEY=your-api-key-here

# 날씨 API 키 (OpenWeatherMap 등)
# WEATHER_API_KEY=your-api-key-here

# ==================== 이메일 설정 (선택사항) ====================
# SMTP 서버 설정 (알림 발송용)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password
# EMAIL_FROM=부성스틸 HR 시스템 <noreply@busung.com>

# ==================== 기타 설정 ====================
# 디버그 모드 (true / false)
DEBUG=false

# ===============================================
# Vercel 배포 시 참고사항:
# ===============================================
# 1. Vercel 대시보드에서 환경변수 설정
#    - Settings > Environment Variables
# 
# 2. 필수 환경변수:
#    - MONGODB_URI (MongoDB Atlas 연결 문자열)
#    - DB_NAME (데이터베이스 이름)
#    - NODE_ENV=production
#
# 3. 환경변수 추가 방법 (CLI):
#    vercel env add MONGODB_URI production
#    vercel env add DB_NAME production
#    vercel env add NODE_ENV production
#
# 4. 보안 주의사항:
#    - .env 파일은 절대 Git에 커밋하지 마세요
#    - .gitignore에 .env 추가 확인
#    - 프로덕션 키는 별도로 안전하게 관리
# ===============================================
